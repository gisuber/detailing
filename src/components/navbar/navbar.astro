---
import Container from "@components/container.astro";
import Link from "@components/ui/link.astro";
import Dropdown from "./dropdown.astro";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";

const menuitems = [
  {
    title: "Questions",
    path: "#",
    children: [
      { title: "Aircraft Detailing Insurance", path: "https://eliteaircraftdetailing.com/blog/aircraft-detailing-insurance" },
      { title: "Aircraft Detailing Supplies", path: "https://eliteaircraftdetailing.com/blog/aircraft-detailing-products-and-supplies" },
      { title: "Aircraft Detailing Technician", path: "https://eliteaircraftdetailing.com/blog/become-an-aircraft-detailing-technician" },
    ],
  },
  {
    title: "Pricing",
    path: "/pricing",
  },
  {
    title: "About",
    path: "/about",
  },
  {
    title: "Blog",
    path: "/blog",
  },
  {
    title: "Contact",
    path: "/contact",
  },
];
---

<Container>
  <header class="flex flex-col lg:flex-row justify-between items-center my-5">
    <Astronav>
      <div class="flex w-full lg:w-auto items-center justify-between">
        <a href="/" class="text-lg"
          ><span class="font-bold text-slate-800">Elite</span><span
            class="text-slate-500">Aircraft Detailing</span
          >
        </a>
        <div class="block lg:hidden">
          <MenuIcon class="w-4 h-4 text-gray-800" />
        </div>
      </div>
      <MenuItems class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0">
        <ul class="flex flex-col lg:flex-row lg:gap-3">
          {
            menuitems.map((item, index) => (
              <>
                {item.children && (
                  <Dropdown
                    title={item.title}
                    children={item.children}
                    lastItem={index === menuitems.length - 1}
                  />
                )}

                {!item.children && (
                  <li>
                    <a
                      href={item.path}
                      class="flex lg:px-3 py-2 text-gray-600 hover:text-gray-900">
                      {item.title}
                    </a>
                  </li>
                )}
              </>
            ))
          }
        </ul>
        <div class="lg:hidden flex items-center mt-3 gap-4">
          <Link href="https://eliteaircraftdetailing.com/about" style="muted" block size="md">About Us</Link>
          <Link href="https://eliteaircraftdetailing.com/contact" size="md" block>Contact Us</Link>
        </div>
      </MenuItems>
    </Astronav>
    <div>
      <div class="hidden lg:flex items-center gap-4">
        <a href="https://eliteaircraftdetailing.com/about">About Us</a>
        <Link href="https://eliteaircraftdetailing.com/contact" size="md">Contact Us</Link>
      </div>
    </div>
  </header>
</Container>
